
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><h4>Flipzone S</h4></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <% if user_signed_in? %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Start Selling</a>
        </li>
        <li class="nav-item">
        <%= link_to "create product", new_product_path , class:"nav-link" %>
        </li>
        <li class="nav-item">
        <%= link_to "Product List", seller_product_list_path , class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "orders", seller_orders_path , class:"nav-link" %>
        </li>
        <li class="nav-item">  
      </ul>
      <% end %>  
    </div>   
      <% if !user_signed_in? %>
      <%= link_to "Sign Up", new_user_registration_path(role: "seller") ,class:"btn btn-primary" %>
      <% else %>
      <%= button_to('Logout', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "btn btn-outline-primary") %>
      <% end %>
  </div>
</nav>